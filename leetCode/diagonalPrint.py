def printDiag(numRows, length):
	counter=0
	some = ""	
	for y in range(numRows):
		some+=str(counter)
		for x in range(length):
			print(counter)
			counter+=1
		print(some)
		some = ""
	for i in range():



printDiag(4,8)