'''
 Zak Mineiko
 leet code 10. Regular Expression Matching
 hard
 incomplete
 runtime:
 memory:
'''

class Solution:
	def isMatch(self, s, p)-> bool:
		# '.' == matches any single char
		# '*' == matches zero or more of the preceding element
		if 
		if s == p:
			return True
		if (p.find('.'))!=-1:
			print('.OFund')
		#print(p.find('*'))
		if (p.find('*'))!=-1:
			print('*found')
	s1 = "aa"
	s2 = "ab"
	p1 = "a"
	p2 = "a*"
	p3 = ".*"
	print(isMatch(0,s1,p1))
		
	print(isMatch(0,s1,p2))
	print(isMatch(0,s2,p3))
	#print(isMatch(0,s1,p1))
